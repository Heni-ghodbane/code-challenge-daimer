import { commonCenterMatrix } from './common-center-matrix';

describe('commonCenterMatrix', () => {
  it('should work', () => {
    expect(commonCenterMatrix()).toEqual('common-center-matrix');
  });
});
